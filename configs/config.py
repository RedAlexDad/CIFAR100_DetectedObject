# Конфигурация для лабораторной работы №2
# Сверточная нейронная сеть для классификации CIFAR-100

# ============================================================================
# ДАННЫЕ
# ============================================================================
# Путь к датасету CIFAR-100 (относительно папки train_cnn)
DATA_DIR = '../cifar-100-python'

# Вариант: классы [8, 32, 90] = ['bicycle', 'flatfish', 'train']
CLASSES = [8, 32, 90]
CLASS_NAMES = ['bicycle', 'flatfish', 'train']
CLASS_NAMES_RU = ['Велосипед', 'Камбала', 'Поезд']

# ============================================================================
# МОДЕЛЬ
# ============================================================================
# Доступные модели: 'base', 'medium', 'deep', 'optimized'
# - base: 2 сверточных слоя (базовая модель из примера)
# - medium: 3 сверточных слоя (улучшенная модель)
# - deep: 6 сверточных слоев (глубокая модель)
# - optimized: 6 сверточных слоев с BatchNorm и Dropout (оптимизированная модель)
MODEL_NAME = 'medium'

# Параметры модели
MODEL_CONFIG = {
    'base': {
        'num_classes': 3,
        'hidden_size': 32,
    },
    'medium': {
        'num_classes': 3,
    },
    'deep': {
        'num_classes': 3,
    },
    'optimized': {
        'num_classes': 3,
        'dropout_rate': 0.3,
    }
}

# ============================================================================
# ОБУЧЕНИЕ
# ============================================================================
TRAIN_CONFIG = {
    'base': {
        'lr': 0.005,          # Learning rate
        'batch_size': 128,     # Размер батча
        'epochs': 100,         # Количество эпох
        'use_scheduler': False,
    },
    'medium': {
        'lr': 0.003,
        'batch_size': 128,
        'epochs': 150,
        'use_scheduler': True,  # CosineAnnealingLR
    },
    'deep': {
        'lr': 0.001,
        'batch_size': 64,
        'epochs': 200,
        'use_scheduler': True,  # CosineAnnealingLR
    },
    'optimized': {
        'lr': 0.001,
        'batch_size': 64,
        'epochs': 200,
        'use_scheduler': True,  # CosineAnnealingLR
    }
}

# ============================================================================
# УСТРОЙСТВО
# ============================================================================
DEVICE = 'cuda'  # 'cuda' или 'cpu'

# ============================================================================
# ПУТИ ДЛЯ СОХРАНЕНИЯ
# ============================================================================
CHECKPOINT_DIR = 'checkpoints'      # Сохранение весов моделей
ONNX_DIR = 'onnx_models'            # ONNX модели
OUTPUT_DIR = 'outputs'              # Графики, матрицы ошибок
CONFIG_DIR = 'configs'              # Конфигурационные файлы
